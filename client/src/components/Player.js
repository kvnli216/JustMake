class Player {
  constructor(username, hand, bid, scores, playedCard) {
    this.username = username;
    this.hand = hand;
    this.bid = bid;
    this.scores = scores;
    this.playedCard = playedCard;
  }
}

export default Player;
